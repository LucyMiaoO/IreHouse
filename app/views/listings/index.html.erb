<div class="jumbotron">
  <h1>To find your dream house in Ireland.</h1>
  <h2>Have a look and enjoy yourself.</h2>
</div>

<div class="center">
  <div class="row">
    <% @listings.each do |listing| %>
      <div class="col-md-3">
        <div class="thumbnail">
          <%= link_to image_tag(listing.image.url), listing %>
          <div class="caption">
            <h3><%= listing.city %></h3>
            <p><%= listing.deposit %> € </p>
            <%= link_to 'Edit', edit_listing_path(listing) , class: "btn btn-link" %>
            <%= link_to 'Delete', listing, method: :delete, data: { confirm: 'Are you sure?' } , class: "btn btn-link"%>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>

<br>


  <%= link_to "New Listing", new_listing_path, class: "btn btn-primary", data: { no_turbolink: true } %>

