<div class="navbar-default sidebar" role="navigation">
  <div class="sidebar-nav navbar-collapse">
    <ul class="nav" id="side-menu">

        <li class="sidebar-search">
            <div class="input-group custom-search-form">
                <input type="text" class="form-control" placeholder="Search...">
                <span class="input-group-btn">
                <button class="btn btn-default" type="button">
                    <i class="fa fa-search"> Search </i>
                </button>
                </span>
            </div>
        </li>

        <li><%= link_to "Welcome", home_path %></li>
        <li><%= link_to "Help", help_path %></li>

        <% if user_signed_in? %>
            <li>
              
              <a href="#">Tenant<span class="fa arrow"></span></a>
                  <ul class="nav nav-second-level">
                    <li>
                      <a href="#">House Infromation  <span class="badge"><%= Listing.count %></span><span class="fa arrow"></span></a>
                        <ul class="nav nav-third-level">
                          <% active = params[:group_id] == nil ? 'active' : '' %>
                          <% Group.all.each do |group| %>
                          <% active = params[:group_id].to_i == group.id ? 'active' : '' %>
                            <li><%= link_to listings_path(group_id: group.id) do %>
                              <%= group.name %> <span class="badge"><%= group.listings.count %></span>
                            <% end %></li>
                          <% end %>
                        </ul>
                    </li>
                    <li><%= link_to "Reserve History", reserves_path %></li>
                  </ul>
            </li>

            <li>
                  <a href="#">Landlord<span class="fa arrow"></span></a>
                  <ul class="nav nav-second-level">
                    <li><%= link_to "My House", landlord_path %></li>
                    <li><%= link_to "Receipt History", receipts_path %></li>
                  </ul>
            </li>

                      
        <% end %>      
    </ul>
  </div>
</div>      